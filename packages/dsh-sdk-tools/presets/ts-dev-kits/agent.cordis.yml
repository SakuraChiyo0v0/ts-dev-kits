# ts-dev-kits 功能包预设
# 挂载 dsh-sdk-tools 插件:选中本预设的会话拥有 bilibili / 网易云 / ffmpeg / lol / vrchat 工具。
# email 默认关(需先配置 smtp 再手动开启,见下方注释)。
# vrchat 默认关(需本机已保存 VRChat 登录态 auth.json 才有查询意义)。
- name: "@sakurachiyo0v0/dsh-sdk-tools"
  config:
    bilibili:
      enabled: true
      # outputDir: "~/Downloads/bilibili"
    netease:
      enabled: true
      # outputDir: "~/Downloads/netease"
      # level: "exhigh"
    ffmpeg:
      enabled: true
    email:
      enabled: false
      # 启用 email 请取消注释并填写 SMTP:
      # smtp:
      #   host: "smtp.example.com"
      #   port: 587
      #   secure: false
      #   user: "your-user"
      #   pass: "your-password"
      #   from: "you@example.com"
    lol:
      enabled: true
    vrchat:
      enabled: false
